- add ability to override the config fallback order (agentPriority) via env. Maybe ORRERY_AGENT_PRIORITY
  - add to readme
- failures.log needs to go to the .agent-work/reports directory (or whatever common work directory)
  - right now they are going to the work/reports directory which is old